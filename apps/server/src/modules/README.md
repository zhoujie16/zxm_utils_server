# Modules 目录

存放业务功能模块。

每个模块代表一个业务功能领域，包含：
- `{模块名}.module.ts`: 模块定义文件
- `{模块名}.controller.ts`: 控制器文件
- `{模块名}.service.ts`: 服务文件
- `dto/`: 数据传输对象目录
- `interfaces/`: 模块内接口定义目录
- `{模块名}.entity.ts`: 实体文件（如果使用 TypeORM）

每个模块应该保持单一职责，模块之间通过依赖注入进行通信。

