# Data 目录

存放应用程序运行时生成的数据文件。

## 说明

- **数据库文件**: SQLite 数据库文件（.db, .sqlite, .sqlite3）应存放在此目录
- **其他运行时数据**: 其他应用程序运行时生成的数据文件也可以存放在此目录

## 注意事项

- 此目录下的文件通常不应提交到版本控制系统
- 数据库文件路径示例: `data/app.db` 或 `data/database.sqlite`

