# 记事本前端应用

专为移动设备设计的渐进式 Web 应用（PWA），采用 React 19 + TypeScript + Vite 构建。提供原生应用般的用户体验，支持离线使用、手势操作和桌面安装。

## 🎯 项目特性

### 核心功能
- **📝 笔记管理**：创建、编辑、删除笔记，支持置顶和搜索
- **🖼️ 图片处理**：图片上传、预览、压缩存储
- **🔐 用户认证**：安全的登录验证和会话管理
- **📱 移动优化**：专为手机浏览器优化的触摸交互
- **🔍 智能搜索**：支持标题和内容的模糊搜索

### PWA 特性
- **离线支持**：Service Worker 缓存，无网络也可使用
- **桌面安装**：可安装到桌面，像原生应用一样启动
- **推送通知**：支持浏览器推送通知
- **自动更新**：应用版本自动检测和更新

### 移动端优化
- **触摸手势**：支持滑动删除、下拉刷新等手势操作
- **响应式设计**：完美适配 320px - 768px+ 屏幕尺寸
- **性能优化**：图片懒加载、虚拟滚动、代码分割
- **浏览器兼容**：支持 iOS Safari、Android Chrome、微信 WebView

## 🛠️ 技术栈

### 前端框架
- **React**：React 19 + TypeScript
- **构建工具**：Vite 7
- **路由管理**：React Router v7
- **状态管理**：@tanstack/react-query
- **样式方案**：Less + CSS Modules

### UI 组件
- **图标库**：lucide-react
- **移动端组件库**：antd-mobile
- **PWA 支持**：vite-plugin-pwa + Workbox

### 开发工具
- **代码检查**：ESLint + @typescript-eslint
- **包管理**：pnpm
- **环境管理**：Vite 环境变量

## 🚀 快速开始

### 环境要求
- Node.js >= 18
- pnpm >= 8
- 现代浏览器（支持 ES2020+）

### 安装依赖
```bash
pnpm install
```

### 配置说明
客户端已移除环境变量配置，使用固定值：
- API 地址：`http://localhost:6680` (在 `src/services/config.ts` 中配置)
- 应用名称：`移动记事本` (在 `index.html` 中配置)

### 开发命令
```bash
# 启动开发服务器
pnpm run dev

# 构建生产版本
pnpm run build

# 预览生产构建
pnpm run preview

# 代码检查
pnpm run lint

# 类型检查
pnpm run type-check
```

### 开发服务器
启动后访问 http://localhost:6681

- **热重载**：代码修改后自动刷新页面
- **错误提示**：编译错误和运行时错误实时提示
- **调试工具**：支持 React DevTools 和 Redux DevTools

## 🏗️ 项目结构

```
src/
├── main.tsx                    # 应用入口
├── App.tsx                     # 根组件
├── router.tsx                  # 路由配置
├── constants.ts                # 应用常量
├── vite-env.d.ts              # Vite 环境类型
├── components/                # 组件库
│   ├── ui/                   # 基础 UI 组件
│   │   ├── Button/          # 按钮组件
│   │   │   ├── index.tsx
│   │   │   └── index.less
│   │   ├── Input/           # 输入框组件
│   │   ├── Modal/           # 弹窗组件
│   │   └── Loading/         # 加载组件
│   └── business/            # 业务组件
│       ├── NoteCard/        # 笔记卡片
│       ├── ImageUploader/   # 图片上传
│       └── SearchBar/       # 搜索栏
├── pages/                    # 页面组件
│   ├── auth/               # 认证页面
│   │   ├── Login.tsx       # 登录页面
│   │   └── Register.tsx    # 注册页面
│   ├── notes/              # 笔记页面
│   │   ├── NoteList.tsx    # 笔记列表
│   │   ├── NoteDetail.tsx  # 笔记详情
│   │   ├── NoteEdit.tsx    # 笔记编辑
│   │   └── NoteSearch.tsx  # 笔记搜索
│   ├── images/             # 图片页面
│   │   ├── ImageList.tsx   # 图片列表
│   │   └── ImagePreview.tsx # 图片预览
│   └── profile/            # 用户页面
│       └── Profile.tsx     # 用户资料
├── services/               # API 服务层
│   ├── api.ts             # API 基础配置
│   ├── auth.ts            # 认证服务
│   ├── notes.ts           # 笔记服务
│   └── images.ts          # 图片服务
├── hooks/                 # 自定义 Hooks
│   ├── useAuth.ts         # 认证 Hook
│   ├── useNotes.ts        # 笔记 Hook
│   ├── useImages.ts       # 图片 Hook
│   └── useLocalStorage.ts # 本地存储 Hook
├── utils/                 # 工具函数
│   ├── date.ts            # 日期处理
│   ├── storage.ts         # 存储处理
│   ├── format.ts          # 格式化处理
│   └── validation.ts      # 验证处理
├── styles/               # 全局样式
│   ├── globals.less      # 全局 Less 样式
│   ├── variables.less    # Less 变量
│   ├── mixins.less       # Less 混入
│   └── themes/           # 主题样式
└── types/               # TypeScript 类型定义
    ├── api.ts           # API 类型
    ├── note.ts          # 笔记类型
    └── user.ts          # 用户类型
```

## 🎨 设计规范

### 设计理念
- **移动优先**：专为移动设备设计，优先考虑触摸操作
- **简洁高效**：界面干净整洁，操作流程简单高效
- **渐进增强**：基础功能稳定，高级功能逐步增强

### 视觉风格
- **配色方案**：浅色简约风格
  - 背景：`#f8fafc`
  - 卡片：`#ffffff`
  - 主色调：`#3b82f6`
  - 边框：`#e5e7eb`

- **字体系统**：系统字体栈，确保最佳可读性
- **间距系统**：4px 基础单位（4px, 8px, 16px, 24px, 32px）
- **圆角统一**：8px（小）、16px（中）、24px（大）

### 组件设计原则
1. **单一职责**：每个组件只负责一个功能
2. **可复用性**：通用组件支持多样化配置
3. **性能优先**：避免不必要的重渲染
4. **无障碍**：支持键盘导航和屏幕阅读器

## 📱 移动端特性

### 触摸交互
- **最小点击区域**：44px × 44px
- **触摸反馈**：触摸时的视觉反馈
- **手势支持**：
  - 滑动删除笔记
  - 下拉刷新列表
  - 侧滑显示操作菜单

### 响应式设计
```less
// 移动端断点
@media (max-width: 768px) {
  /* 移动端样式 */
}

@media (min-width: 769px) {
  /* 平板和桌面样式 */
}
```

### 性能优化
- **图片懒加载**：使用 Intersection Observer
- **虚拟滚动**：长列表使用 react-window
- **代码分割**：按路由和功能模块分割
- **资源预加载**：关键资源 preload 和 prefetch

## 🔐 认证与安全

### JWT 认证
```typescript
// 认证状态管理
const { user, login, logout } = useAuth();

// 自动刷新令牌
useQuery({
  queryKey: ['refresh-token'],
  queryFn: refreshToken,
  refetchInterval: 30 * 60 * 1000, // 30分钟
});
```

### 安全特性
- **令牌存储**：安全的本地存储方案
- **自动登出**：令牌过期自动登出
- **请求拦截**：自动添加认证头
- **错误处理**：统一的认证错误处理

## 📊 状态管理

### React Query 配置
```typescript
// 全局配置
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5分钟
      cacheTime: 10 * 60 * 1000, // 10分钟
    },
  },
});
```

### 本地状态
- **表单状态**：使用 useState 管理表单数据
- **UI 状态**：使用 Context 管理全局 UI 状态
- **会话状态**：使用 localStorage 持久化用户会话

## 🌐 PWA 配置

### Service Worker
```typescript
// vite.config.ts
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/api\./i,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'api-cache',
              expiration: {
                maxEntries: 100,
                maxAgeSeconds: 60 * 60 * 24, // 24小时
              },
            },
          },
        ],
      },
    }),
  ],
});
```

### 缓存策略
- **API 请求**：NetworkFirst，24小时过期
- **静态资源**：CacheFirst，30天过期
- **图片资源**：CacheFirst，30天过期
- **动态内容**：NetworkOnly，不缓存

## 🔧 开发工具

### 推荐工具
- **代码编辑**：VS Code + ES7+ React/Redux/React-Native snippets
- **调试工具**：React Developer Tools、React Query DevTools
- **性能分析**：Chrome DevTools、Lighthouse
- **移动端调试**：Chrome DevTools Device Mode

### VS Code 配置
```json
// .vscode/settings.json
{
  "typescript.preferences.importModuleSpecifier": "relative",
  "emmet.includeLanguages": {
    "typescript": "html",
    "typescriptreact": "html"
  },
  "files.associations": {
    "*.less": "less"
  }
}
```

## 🚀 部署指南

### 构建配置
```bash
# 生产构建
pnpm run build

# 分析构建包大小
pnpm run build --analyze
```

### 部署到静态托管
```bash
# 部署到 Vercel
vercel --prod

# 部署到 Netlify
netlify deploy --prod --dir=dist

# 部署到 GitHub Pages
pnpm run build
pnpm run deploy
```

### 生产环境配置
生产环境的 API 地址需要在 `src/services/config.ts` 中手动修改：
```typescript
export const API_CONFIG = {
  baseURL: 'https://api.yourapp.com', // 修改为实际的生产环境地址
  timeout: 10000,
} as const;
```

## 📈 性能优化

### 关键指标
- **首次内容绘制 (FCP)**：< 1.5秒
- **最大内容绘制 (LCP)**：< 2.5秒
- **首次输入延迟 (FID)**：< 100毫秒
- **累积布局偏移 (CLS)**：< 0.1

### 优化策略
1. **代码分割**：按路由和功能分割代码
2. **资源压缩**：图片和代码压缩
3. **缓存策略**：合理的缓存配置
4. **预加载**：关键资源预加载

## 🧪 测试策略

### 测试类型
- **单元测试**：组件和函数逻辑测试
- **集成测试**：组件交互测试
- **E2E 测试**：用户流程测试
- **视觉回归测试**：UI 一致性测试

### 测试命令
```bash
# 运行单元测试
pnpm run test

# 运行 E2E 测试
pnpm run test:e2e

# 生成覆盖率报告
pnpm run test:coverage
```

## 🤝 贡献指南

### 开发流程
1. Fork 项目到个人仓库
2. 创建功能分支：`git checkout -b feature/new-feature`
3. 开发并测试功能
4. 提交代码：`git commit -m 'feat: add new feature'`
5. 推送分支：`git push origin feature/new-feature`
6. 创建 Pull Request

### 代码规范
- **提交信息**：遵循 Conventional Commits 规范
- **代码风格**：使用 ESLint 和 Prettier
- **类型安全**：严格的 TypeScript 配置
- **组件规范**：遵循 React 最佳实践

### 提交信息格式
```
type(scope): description

feat(auth): add login functionality
fix(ui): resolve button styling issue
docs(readme): update installation guide
```

## 📚 API 文档

### 服务层架构
```typescript
// API 基础配置
export const api = {
  baseURL: 'http://localhost:6680', // 固定配置，生产环境需手动修改
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
};

// 请求拦截器
api.interceptors.request.use((config) => {
  const token = getAuthToken();
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

### 错误处理
```typescript
// 统一错误处理
export const handleApiError = (error: ApiError) => {
  switch (error.status) {
    case 401:
      // 处理认证错误
      logout();
      break;
    case 404:
      // 处理资源不存在
      showNotification('资源不存在');
      break;
    default:
      showNotification('网络错误，请重试');
  }
};
```

## 🔍 故障排除

### 常见问题

#### 开发环境问题
- **端口冲突**：修改 `vite.config.ts` 中的端口配置
- **热重载失效**：检查文件监听限制和防火墙设置
- **依赖安装失败**：尝试清理缓存：`pnpm store prune`

#### 构建问题
- **内存溢出**：增加 Node.js 内存限制：`export NODE_OPTIONS="--max-old-space-size=4096"`
- **构建缓慢**：启用并行构建和增量构建
- **静态资源问题**：检查 public 目录和资源路径

#### 运行时问题
- **白屏问题**：检查控制台错误和网络请求
- **路由问题**：确认 History API 配置正确
- **PWA 问题**：检查 Service Worker 注册和缓存策略

### 调试技巧
1. **控制台日志**：使用 `console.log` 和 React DevTools
2. **网络监控**：使用 Network 面板监控 API 请求
3. **性能分析**：使用 Performance 面板分析性能瓶颈
4. **移动端调试**：使用 Chrome DevTools 远程调试

## 📄 许可证

本项目采用 MIT 许可证，详情请查看 [LICENSE](../LICENSE) 文件。

## 🆘 支持

如有问题或建议，请：

1. 查看 [常见问题](docs/FAQ.md)
2. 搜索现有的 [Issues](../../issues)
3. 创建新的 Issue 描述问题
4. 联系项目维护者

---

**🎉 感谢使用记事本前端应用！**