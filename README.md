# ZXM Toolkit Admin

个人工具集合管理平台，基于 Monorepo 架构的全栈 Web 应用，包含前端管理后台和后端 API 服务。

## 📋 项目结构

```
zxm-toolkit-admin/
├── apps/
│   ├── admin/          # 前端管理后台 (UmiJS + React + Ant Design)
│   └── server/         # 后端 API 服务 (NestJS + TypeScript + SQLite)
├── doc-server-api/     # API 接口文档
└── package.json        # 根配置文件 (Yarn Workspaces)
```

## 🛠 技术栈

### 前端 (Admin)
- **框架**: UmiJS 4.x + React 19
- **UI 组件库**: Ant Design 5.x
- **状态管理**: SWR
- **语言**: TypeScript 5.6
- **构建工具**: Mako

### 后端 (Server)
- **框架**: NestJS 11.x
- **语言**: TypeScript 5.7
- **数据库**: SQLite3 (better-sqlite3)
- **ORM**: TypeORM 0.3.x
- **API 文档**: Swagger/OpenAPI
- **认证**: JWT

## 🚀 快速开始

### 前置要求

- Node.js >= 20.0.0
- Yarn >= 1.22.22

### 安装依赖

```bash
# 安装所有依赖（包括前端和后端）
yarn install
```

### 环境配置

#### 后端环境配置

```bash
cd apps/server
cp env.example .env
```

编辑 `apps/server/.env` 文件，根据实际需求修改配置项。

### 启动项目

#### 方式一：分别启动（推荐）

```bash
# 启动前端（端口 8008）
cd apps/admin
yarn dev

# 启动后端（端口 8010）
cd apps/server
yarn start:dev
```

#### 方式二：使用根目录脚本

```bash
# 启动前端
yarn dev:admin

# 启动后端
yarn dev:server
```

### 访问地址

- **前端管理后台**: http://localhost:8008
- **后端 API**: http://localhost:8010/api
- **Swagger API 文档**: http://localhost:8010/docs (仅开发环境)

## 📚 项目说明

### 前端 (apps/admin)

基于 UmiJS 和 Ant Design 构建的管理后台系统，提供现代化的 UI 界面和良好的开发体验。

详细文档请查看 [apps/admin/README.md](./apps/admin/README.md)

### 后端 (apps/server)

基于 NestJS 构建的 RESTful API 服务，采用模块化架构设计，提供完整的业务功能。

详细文档请查看 [apps/server/README.md](./apps/server/README.md)

### API 文档

项目 API 文档位于 `doc-server-api/` 目录，包含详细的接口说明和使用示例。

- [API 文档总览](./doc-server-api/README.md)
- [登录认证接口](./doc-server-api/login/login.md)

## 🛠 开发脚本

| 脚本 | 说明 |
|------|------|
| `yarn dev:admin` | 启动前端开发服务器 |
| `yarn dev:server` | 启动后端开发服务器 |
| `yarn admin:build` | 构建前端生产版本 |
| `yarn server:build` | 构建后端生产版本 |
| `yarn format` | 格式化所有代码文件 |

## 📝 开发规范

项目遵循统一的开发规范：

- **代码规范**: ESLint + Prettier
- **命名规范**: 类型定义以 `I` 开头（如 `IUserData`）
- **编程范式**: 函数式编程，优先使用纯函数
- **组件设计**: 单一职责，逻辑与 UI 分离
- **文件组织**: 单个文件不超过 300 行

详细规范请参考项目根目录下的 `.cursor/rules/` 目录。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request。

## 📄 许可证

UNLICENSED

