---
description: 前端开发规范 - React + UmiJS + Ant Design
globs: ['apps/admin/**/*']
alwaysApply: false
---

# ⚛️ 前端开发规范 (Admin)

## 🎯 核心原则

### 1. 组件开发

- ✅ **函数组件**: 使用函数组件 + Hooks，避免类组件
- ✅ **命名规范**: 组件使用PascalCase，Props接口使用`I`前缀
- ✅ **状态管理**: 本地状态用useState，全局状态用UmiJS Model，服务器状态用SWR
- ✅ **组件组合**: 使用组件组合而非继承
- ✅ **逻辑分离**: 逻辑与UI分离，使用hooks保持组件纯净
- ✅ **组件目录**: 每个组件需要单独目录，包含 `index.tsx`，有样式时需包含 `index.less`，如果逻辑复杂需要组件拆分

### 2. 性能优化

- ✅ **React.memo**: 对高开销组件使用记忆化
- ✅ **useMemo/useCallback**: 合理使用记忆化函数和值
- ✅ **懒加载**: 路由和组件按需加载
- ✅ **避免重渲染**: 优化依赖数组，减少不必要更新

## 📋 Ant Design使用规范

### 表格组件

- ✅ 必须指定rowKey
- ✅ 添加loading状态
- ✅ 配置分页和横向滚动
- ✅ 使用中文分页提示

### 表单组件

- ✅ 使用vertical布局

### 抽屉组件

- ✅ 设置合适的宽度

## 🔄 数据获取规范

### SWR使用最佳实践

- ✅ 自定义Hook封装API调用
- ✅ 配置去重间隔和聚焦验证
- ✅ 提供刷新功能
- ✅ 统一错误处理

## 🎨 样式和布局

### 样式组织

- ✅ **样式方案**: 优先使用 less
- ✅ **样式模块化**: 组件样式与组件文件同级

### BEM 命名规范

- ✅ **块（Block）**: `.user-card`
- ✅ **元素（Element）**: `.user-card__avatar`、`.user-card__name`
- ✅ **修饰符（Modifier）**: `.user-card--active`、`.user-card__avatar--large`
- ✅ **命名规则**: 使用小写字母和连字符，避免下划线（除 BEM 分隔符外）

### DOM 层级注意事项

- ✅ **嵌套深度**: 避免过深的 DOM 嵌套（建议不超过 5 层）
- ✅ **语义化标签**: 合理使用语义化 HTML5 标签
- ✅ **选择器性能**: 注意选择器的性能，避免过于复杂的嵌套选择器
- ✅ **样式作用域**: 保持样式的作用域清晰，避免样式污染
- ✅ **唯一命名**: 样式最外层 class 尽量命名唯一，避免与其他组件样式冲突

## 📊 图表和图标

### 图表组件

- ✅ **图表库**: 前端图表组件统一使用 recharts 库
- ✅ **最佳实践**: 遵循 recharts 最佳实践
- ✅ **主题一致**: 保持图表样式与 Ant Design 主题一致

### 图标组件

- ✅ **图标库**: 前端图标统一使用 lucide-react 库
- ✅ **样式自定义**: 可通过 `size`、`color`、`strokeWidth` 等属性自定义样式
- ✅ **混合使用**: 与 `@ant-design/icons` 可同时使用，按需选择

## 🚨 强制要求

1. **禁止ProComponents**: 不使用ProComponents，保持项目简洁，不使用 PageContainer 组件
2. **性能优化**: 列表组件必须添加key属性，大数据量使用虚拟滚动
3. **禁止构建验证**: 修改admin项目代码后，不需要运行build命令进行验证

> 其他通用规范请参考 `base.mdc`