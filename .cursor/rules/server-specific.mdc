---
description: 后端开发规范 - Go + Gin + SQLite
globs: ['apps/server-go/**/*']
alwaysApply: false
---

# 后端开发规范 (Server Go)

## 核心原则

### 架构设计

- 按现有分层组织代码：`handler -> service -> repository -> model`
- HTTP 相关逻辑放在 `internal/http`
- 业务逻辑放在 `internal/service`
- 数据访问放在 `internal/repository`
- 数据结构放在 `internal/model` 和 `internal/dto`

### 响应规范

- 使用标准 HTTP 状态码
- 成功响应直接返回业务数据
- 错误响应使用 `{ "message": "..." }`

## DTO 与验证规范

- 外部输入在 handler 层完成基础解析和校验
- 复杂业务校验放在 service 层
- 分页接口支持 `page`、`limit`
- 时间范围接口应明确时间单位和格式

## SQLite / GORM 规范

- 表结构使用 `internal/model` 中的 GORM model 定义
- 字段名应与既有 JSON/API 字段保持一致
- 需要查询的字段添加索引
- 数据库自动同步由 `database.synchronize` 控制

## 查询规范

- 分页查询返回 `data` 和 `pagination`
- 列表查询需要明确排序规则
- 复杂查询优先封装在 repository

## 强制要求

1. Go 代码必须使用 `gofmt`
2. 需要认证的接口必须挂载 JWT 中间件
3. 接口新增或变更必须同步更新 `doc-server-api`
4. 数据库字段新增或变更必须同步更新 `doc-server-db`
5. 不要提交密钥、本地数据库文件或构建产物

> 其他通用规范请参考 `base.mdc`
